# receivers  (no view)

# netwerk_project = NULL  # all projects
# netwerk_project = "my_project"  # single project
# netwerk_project = c("my_project1", "my_project2")  # multiple project
get_receivers <- function(connection,
                          netwerk_project = NULL) {
  NULL
}
